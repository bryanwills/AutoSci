# edges.yaml — Relation contract for graph edges (incl. citations).
#
# Each edge type declares:
#   endpoints   { from: <kind>, to: <kind> }  -- '*' means any kind
#   direction   directed | symmetric           -- symmetric edges are stored once
#                                                 with sorted endpoints
#   workflow    which subsystem owns this edge type
#               (ingest | evidence | experiment | idea | provenance | citation)
#   attributes  schema for edge-record fields beyond from/to/type
#
# Storage location of each edge type is decided in conventions.yaml::edge_storage.

# -----------------------------------------------------------------------------
# Paper ↔ Paper semantic edges  (written by /ingest)
# -----------------------------------------------------------------------------

# 4 irreducible paper↔paper semantic edges. The 4 removed in this round
# (improves_on / compares_against / surveys / complementary_to) were judged
# redundant:
#   - improves_on    ⊆ builds_on (subset; merged)
#   - compares_against ⊆ cites    (every comparison already cites)
#   - surveys        ⇐ papers.contribution_type contains "survey" (derivable)
#   - complementary_to ≈ same_problem_as (boundary fuzzy in practice)

same_problem_as:
  endpoints: { from: papers, to: papers }
  direction: symmetric
  workflow:  ingest
  attributes:
    confidence: { type: enum, values: [high, medium, low], required: true }
    evidence:   { type: str, required: true }

similar_method_to:
  endpoints: { from: papers, to: papers }
  direction: symmetric
  workflow:  ingest
  attributes:
    confidence: { type: enum, values: [high, medium, low], required: true }
    evidence:   { type: str, required: true }

builds_on:
  endpoints: { from: papers, to: papers }
  direction: directed
  workflow:  ingest
  attributes:
    confidence: { type: enum, values: [high, medium, low], required: true }
    evidence:   { type: str, required: true }

challenges:
  endpoints: { from: papers, to: papers }
  direction: directed
  workflow:  ingest
  attributes:
    confidence: { type: enum, values: [high, medium, low], required: true }
    evidence:   { type: str, required: true }

# -----------------------------------------------------------------------------
# Paper → Concept semantic edges  (written by /ingest)
# -----------------------------------------------------------------------------

introduces_concept:
  endpoints: { from: papers, to: concepts }
  direction: directed
  workflow:  ingest
  attributes:
    confidence: { type: enum, values: [high, medium, low], required: true }
    evidence:   { type: str, required: true }

uses_concept:
  endpoints: { from: papers, to: concepts }
  direction: directed
  workflow:  ingest
  attributes:
    confidence: { type: enum, values: [high, medium, low], required: true }
    evidence:   { type: str, required: true }

extends_concept:
  endpoints: { from: papers, to: concepts }
  direction: directed
  workflow:  ingest
  attributes:
    confidence: { type: enum, values: [high, medium, low], required: true }
    evidence:   { type: str, required: true }

critiques_concept:
  endpoints: { from: papers, to: concepts }
  direction: directed
  workflow:  ingest
  attributes:
    confidence: { type: enum, values: [high, medium, low], required: true }
    evidence:   { type: str, required: true }

# -----------------------------------------------------------------------------
# Concept ↔ Concept typed relations  (written by /ingest, symmetric)
# -----------------------------------------------------------------------------

shares_mechanism_with:
  endpoints: { from: concepts, to: concepts }
  direction: symmetric
  workflow:  ingest
  attributes:
    confidence: { type: enum, values: [high, medium, low], required: true }
    evidence:   { type: str, required: true }

shares_assumption_with:
  endpoints: { from: concepts, to: concepts }
  direction: symmetric
  workflow:  ingest
  attributes:
    confidence: { type: enum, values: [high, medium, low], required: true }
    evidence:   { type: str, required: true }

related_problem_formulation:
  endpoints: { from: concepts, to: concepts }
  direction: symmetric
  workflow:  ingest
  attributes:
    confidence: { type: enum, values: [high, medium, low], required: true }
    evidence:   { type: str, required: true }

contrasts_with_concept:
  endpoints: { from: concepts, to: concepts }
  direction: symmetric
  workflow:  ingest
  attributes:
    confidence: { type: enum, values: [high, medium, low], required: true }
    evidence:   { type: str, required: true }

# -----------------------------------------------------------------------------
# Paper → Method semantic edges  (written by /ingest)
# -----------------------------------------------------------------------------

proposes_method:
  endpoints: { from: papers, to: methods }
  direction: directed
  workflow:  ingest
  attributes:
    confidence: { type: enum, values: [high, medium, low], required: true }
    evidence:   { type: str, required: true }

applies_method:
  endpoints: { from: papers, to: methods }
  direction: directed
  workflow:  ingest
  attributes:
    confidence: { type: enum, values: [high, medium, low], required: true }
    evidence:   { type: str, required: true }

extends_method:
  endpoints: { from: papers, to: methods }
  direction: directed
  workflow:  ingest
  attributes:
    confidence: { type: enum, values: [high, medium, low], required: true }
    evidence:   { type: str, required: true }

# -----------------------------------------------------------------------------
# Paper → Foundation contribution edge  (written by /ingest)
# -----------------------------------------------------------------------------

contributes_to_foundation:
  endpoints: { from: papers, to: foundations }
  direction: directed
  workflow:  ingest
  attributes:
    confidence: { type: enum, values: [high, medium, low], required: true }
    evidence:   { type: str, required: true }

# -----------------------------------------------------------------------------
# Workflow edges (evidence / experiments / ideas / provenance)
# Endpoints stay broad because these span multiple entity kinds.
# -----------------------------------------------------------------------------

supports:
  endpoints: { from: '*', to: '*' }
  direction: directed
  workflow:  evidence
  attributes:
    evidence:      { type: str }
    source_path:   { type: str }
    source_span:   { type: str }
    metric_name:   { type: str }
    metric_value:  { type: str }
    artifact_hash: { type: str }

contradicts:
  endpoints: { from: '*', to: '*' }
  direction: directed
  workflow:  evidence
  attributes:
    evidence:      { type: str }
    source_path:   { type: str }
    source_span:   { type: str }
    metric_name:   { type: str }
    metric_value:  { type: str }
    artifact_hash: { type: str }

tested_by:
  endpoints: { from: '*', to: '*' }
  direction: directed
  workflow:  experiment
  attributes:
    evidence:      { type: str }
    source_path:   { type: str }
    source_span:   { type: str }
    metric_name:   { type: str }
    metric_value:  { type: str }
    artifact_hash: { type: str }

invalidates:
  endpoints: { from: '*', to: '*' }
  direction: directed
  workflow:  experiment
  attributes:
    evidence:      { type: str }
    source_path:   { type: str }
    source_span:   { type: str }
    metric_name:   { type: str }
    metric_value:  { type: str }
    artifact_hash: { type: str }

addresses_gap:
  endpoints: { from: '*', to: '*' }
  direction: directed
  workflow:  idea
  attributes:
    evidence: { type: str }

derived_from:
  endpoints: { from: '*', to: '*' }
  direction: directed
  workflow:  provenance
  attributes:
    evidence: { type: str }

inspired_by:
  endpoints: { from: '*', to: '*' }
  direction: directed
  workflow:  idea
  attributes:
    evidence: { type: str }

# -----------------------------------------------------------------------------
# Bibliographic citation
# -----------------------------------------------------------------------------

cites:
  endpoints: { from: papers, to: papers }
  direction: directed
  workflow:  citation
  attributes:
    source: { type: enum, values: [semantic_scholar, parsed_bib, manual], required: true }
    date:   { type: date }
